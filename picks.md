---
layout: default
title: Matchup Picks
permalink: /picks/
description: Weekly Road to Glory matchup voting, verified results, and the season Picks Leaderboard.
---

{% assign picks = site.data.generated.picks %}
{% include page-hero.html eyebrow="Make the call" title="Matchup Picks" description="One point for a correct winner. No confidence points, betting lines, or standings shortcuts—just the league calling its shots." compact=true %}

<section class="shell-content vote-section">
  <div class="section-heading"><div><p class="eyebrow">This week's slate</p><h2>Weekly Matchups</h2><p>One complete ballot covers every matchup. Percentages stay hidden until the commissioner locks and publishes the week.</p></div>{% if picks.current_week.form_url and picks.current_week.state == 'open' %}<a class="button button--gold" href="{{ picks.current_week.form_url }}" target="_blank" rel="noopener noreferrer">Submit Week {{ picks.current_week.week }} Picks <span aria-hidden="true">↗</span></a>{% endif %}</div>
  {% if picks.current_week %}<div class="community-status"><strong>Week {{ picks.current_week.week }} · {% include community-state.html state=picks.current_week.state %}</strong><span>{% if picks.current_week.lock_at %}Lock: <time datetime="{{ picks.current_week.lock_at }}">{{ picks.current_week.lock_at | date: '%b %-d, %-I:%M %p' }} Eastern</time>{% else %}Lock time will be posted with the ballot.{% endif %}</span></div>{% endif %}
  {% if picks.current_week %}<div class="pick-card-grid">{% for matchup in picks.current_week.matchups %}<article class="pick-card"><div class="pick-card__top"><span>Week {{ matchup.week }}</span><strong>{% if matchup.winner_status == 'verified' %}Final{% elsif matchup.winner_status == 'no_contest' %}No contest{% elsif picks.current_week.state == 'open' %}Voting open{% elsif picks.current_week.state == 'locked' %}Picks locked{% else %}Upcoming{% endif %}</strong></div><div class="pick-card__teams">{% for team in matchup.participants %}<div class="{% if matchup.winner_franchise_id == team.franchise_id %}is-winner{% endif %}"><img src="{{ team.identity_image | relative_url }}" alt=""><a href="{{ team.path | relative_url }}">{{ team.display_name }}</a>{% assign team_result = matchup.pick_results | where: 'franchise_id', team.franchise_id | first %}{% if team_result %}<small>{{ team_result.vote_count }} picks · {{ team_result.percentage | times: 100 | round: 1 }}%</small>{% endif %}</div>{% unless forloop.last %}<b>VS</b>{% endunless %}{% endfor %}</div></article>{% endfor %}</div>{% else %}<div class="vote-empty"><span aria-hidden="true">W</span><div><h3>The current slate is not available yet</h3><p>Weekly cards will appear only after the current week’s schedule is available.</p></div></div>{% endif %}
</section>

<section class="content-section content-section--navy vote-section"><div class="wrap">
  <div class="section-heading section-heading--light"><div><p class="eyebrow">Every correct call counts</p><h2>Season Picks Leaderboard</h2><p>Most correct picks leads the board, followed by accuracy. Matching totals share a rank.</p></div></div>
  {% if picks.leaderboard.size > 0 %}<p class="table-scroll-note table-scroll-note--light">Swipe horizontally to see the full leaderboard.</p><div class="vote-table-wrap" role="region" aria-label="Season Picks Leaderboard" tabindex="0"><table class="vote-table picks-table"><thead><tr><th>Rank</th><th>Manager</th><th>Correct</th><th>Incorrect</th><th>Total</th><th>Accuracy</th><th>Weekly wins</th></tr></thead><tbody>{% for manager in picks.leaderboard %}<tr><td class="vote-rank">{{ manager.rank }}</td><td>{{ manager.display_name }}</td><td>{{ manager.correct }}</td><td>{{ manager.incorrect }}</td><td>{{ manager.total_picks }}</td><td>{{ manager.accuracy | times: 100 | round: 1 }}%</td><td>{{ manager.weekly_wins }}</td></tr>{% endfor %}</tbody></table></div>{% else %}<div class="vote-empty vote-empty--dark"><span aria-hidden="true">1</span><div><h3>Results begin after verified games</h3><p>No pick is scored until Yahoo confirms the actual winner.</p></div></div>{% endif %}
</div></section>

<section class="shell-content vote-section"><div class="section-heading"><div><p class="eyebrow">Week by week</p><h2>Pick Results Archive</h2><p>Locked weeks preserve aggregate selections. Final weeks add verified Yahoo winners and the manager scoreboard.</p></div></div>{% if picks.weekly_results.size > 0 %}<div class="pick-week-archive">{% for week in picks.weekly_results reversed %}<details><summary><span>{{ week.season }} · Week {{ week.week }}</span><strong>{% include community-state.html state=week.state %}</strong><small>{{ week.ballots_counted }} ballots · {{ week.matchups.size }} matchups</small></summary><div class="pick-week-archive__body">{% if week.weekly_winners.size > 0 %}<p><strong>Weekly winner{% if week.weekly_winners.size > 1 %}s{% endif %}:</strong> {% for winner in week.weekly_winners %}{{ winner.display_name }} ({{ winner.correct }}–{{ winner.incorrect }}){% unless forloop.last %}, {% endunless %}{% endfor %}</p>{% endif %}<ul>{% for matchup in week.matchups %}<li><span>{{ matchup.participants[0].display_name }} vs. {{ matchup.participants[1].display_name }}</span><strong>{% if matchup.winner_status == 'verified' %}{% assign winner = matchup.participants | where: 'franchise_id', matchup.winner_franchise_id | first %}{{ winner.display_name }} won{% elsif matchup.winner_status == 'no_contest' %}No contest{% else %}Pending Yahoo result{% endif %}</strong></li>{% endfor %}</ul>{% if week.manager_results.size > 0 %}<div class="vote-table-wrap" role="region" aria-label="Week {{ week.week }} manager Pick’em results" tabindex="0"><table class="vote-table"><thead><tr><th>Manager</th><th>Correct</th><th>Incorrect</th><th>Accuracy</th></tr></thead><tbody>{% for manager in week.manager_results %}<tr><td>{{ manager.display_name }}</td><td>{{ manager.correct }}</td><td>{{ manager.incorrect }}</td><td>{% if manager.accuracy != null %}{{ manager.accuracy | times: 100 | round: 1 }}%{% else %}—{% endif %}</td></tr>{% endfor %}</tbody></table></div>{% endif %}</div></details>{% endfor %}</div>{% else %}<div class="vote-empty"><span aria-hidden="true">—</span><div><h3>No weekly results are archived</h3><p>The archive remains empty until real ballots are locked. Picks are scored only after Yahoo verifies winners.</p></div></div>{% endif %}<div class="vote-back-links"><a class="text-link" href="{{ '/votes/' | relative_url }}"><span aria-hidden="true">←</span> League Votes</a><a class="text-link" href="{{ '/power-rankings/' | relative_url }}">Power Rankings <span aria-hidden="true">→</span></a></div></section>
